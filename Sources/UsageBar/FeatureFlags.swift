/// Compile-time feature gates.
///
/// The Usage window complements the menu-bar quota view with locally
/// reconstructed token accounting and cost estimates.
enum FeatureFlags {
    static let dashboardEnabled = true
}
